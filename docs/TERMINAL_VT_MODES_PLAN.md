# Terminal VT modes slice

This temporary staging note exists only to anchor the stacked branch creation.
The implementation branch adds conservative capability declarations, DEC
private-mode tracking, application cursor keys, cursor visibility and bracketed
paste hardening. It will be removed with the temporary applicator files after
validation.
