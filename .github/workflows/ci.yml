name: CI

on:
  push:
  pull_request:

permissions:
  contents: write

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  MODULE_COVERAGE_FAIL_UNDER: "75.0"

jobs:
  apply-windows-terminal-fix:
    if: github.event_name == 'pull_request' && github.head_ref == 'agent/windows-terminal-e2e'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout migration branch
        uses: actions/checkout@v5
        with:
          ref: agent/windows-terminal-e2e
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Apply exact ConPTY fix
        run: python tools/apply_windows_conpty_fix.py

      - name: Validate focused terminal tests
        shell: bash
        run: |
          set -o pipefail
          python -m unittest discover -s tests -p "test_terminal_session.py" -v > focused-tests.log 2>&1 || {
            tail -120 focused-tests.log
            exit 1
          }

      - name: Upload focused test log
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: focused-terminal-tests
          path: focused-tests.log
          retention-days: 1

      - name: Validate repository quality
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[test]"
          python tools/qa.py --skip-tests
          python -m ruff check --select F821 retrotui tests tools

      - name: Publish clean fix commit
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm tools/apply_windows_conpty_fix.py
          git add retrotui/core/terminal_session.py tests/test_terminal_session.py .github/workflows/ci.yml
          git commit -m "Fix real Windows ConPTY reads"
          git push origin HEAD:agent/windows-terminal-e2e

  quality:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
        python-version: ["3.10", "3.12", "3.14"]

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install package and test dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[test]"

      - name: Run real Windows ConPTY smoke test
        if: runner.os == 'Windows'
        env:
          RETROTUI_WINDOWS_E2E: "1"
        run: >-
          python -m unittest discover
          -s tests
          -p "test_windows_terminal_e2e.py"
          -v

      - name: Run repository quality checks
        run: python tools/qa.py --skip-tests

      - name: Reject undefined Python names
        run: python -m ruff check --select F821 retrotui tests tools

      - name: Reject unused RetroTUI composition code
        run: python -m ruff check --select F401,F811,F841 retrotui/core/app.py

      - name: Run unittest suite
        run: python -m unittest discover -s tests -v

      - name: Run pytest suite
        if: ${{ !cancelled() }}
        run: python -m pytest tests -q

  coverage:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install package and test dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[test]"

      - name: Enforce module coverage floor
        run: >-
          python tools/report_module_coverage.py
          --quiet-tests
          --top 20
          --fail-under "${{ env.MODULE_COVERAGE_FAIL_UNDER }}"
