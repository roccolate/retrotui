[plugin]
id = "service-manager"
name = "Service Manager"
version = "0.1.0"
description = "List and control systemd services (example). Uses systemctl if available."
menu_category = "System"

[plugin.window]
default_width = 80
default_height = 18
resizable = true
