[plugin]
id = "git-status"
name = "Git Status"
version = "0.1.0"
description = "Show branch and recent commits for a repo (example)."
menu_category = "Development"

[plugin.window]
default_width = 70
default_height = 16
resizable = true
